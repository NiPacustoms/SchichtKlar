# JobFlow – Claude Code Konfiguration

## Sprache
Alle Antworten immer auf **Deutsch**.

## Projekt-Überblick
- Next.js 15 (App Router), TypeScript 5, MUI v5, Firebase, TanStack React Query
- ESLint Flat Config (`eslint.config.mjs`) – `eslint: { ignoreDuringBuilds: true }` in `next.config.js`
- Build-Bedingung: `NEXT_PUBLIC_APP_ENV=development` muss gesetzt sein, sonst PRODUCTION BLOCKER

## Entwicklungsbranch
`claude/elegant-gates-c38nP`
