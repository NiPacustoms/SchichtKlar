rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Testmodus: Chat-spezifische Collections offen
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
