{
  "name": "functions",
  "scripts": {
    "postinstall": "node scripts/fix-object-inspect.js",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "date-fns-tz": "^3.2.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "nodemailer": "^7.0.10",
    "pdf-lib": "^1.17.1",
    "puppeteer": "^22.4.1",
    "resend": "^6.9.2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "private": true
}
